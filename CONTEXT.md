# Douyin Keeper domain context

## 会话

会话是产品管理的唯一关系对象，表示账号在抖音消息面板中看到的一条对话。会话可以是直接会话，也可以是群聊；类型只描述平台路由，不改变“会话”这一产品概念。
会话不区分 consumer/creator 渠道；对外只按平台会话身份、会话类型和对端身份管理。

## 直接会话

直接会话是会话的一种平台类型。它可能对应一个好友，也可能对应一个尚未建立好友关系的对端。只要消息面板提供稳定路由，就可以配置火花维护；好友关系不是前置条件。

## 群聊会话

群聊会话是会话的一种平台类型。群名和成员信息来自消息面板，不要求存在好友关系，与直接会话共同参与火花维护。

## 火花维护

火花维护面向会话，而不是好友实体。当前任务表仍保留 `friend_id` 作为兼容存储字段；它指向由会话快照物化出的可发送目标，群聊使用稳定的内部投影键，实际发送始终使用平台会话 ID。

## 会话同步

会话同步是从抖音消息面板读取完整会话快照的唯一索引动作。产品不再把粉丝/好友列表作为独立数据源。
